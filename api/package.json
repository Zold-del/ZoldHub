{
  "name": "zoldstudio-api",
  "version": "1.0.0",
  "description": "API pour ZoldStudio utilisant MongoDB",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "echo \"No tests specified yet\" && exit 0",
    "lint": "echo \"No linting configured yet\" && exit 0",
    "build": "echo \"Checking project structure...\" && node -e \"require('./src/index')\"",
    "deploy": "echo \"Deployment should be handled by CI/CD pipelines\""
  },
  "keywords": [
    "api",
    "mongodb",
    "express",
    "node"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.15.0",
    "mongoose": "^7.0.3"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
